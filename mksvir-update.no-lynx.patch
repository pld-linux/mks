--- mksvir-update	Fri Jul  4 20:47:03 2003
+++ mksvir-update	Fri Nov  7 13:00:31 2003
@@ -61,7 +61,7 @@
 # remote file timestamp check
 
 [ -n "$DEBUG" ] && echo "Retrieving timestamp"
-lynx -reload -dump -head "$MKS_URL" | grep Last-Modified >"$TIMESTAMP"
+wget -S --spider "$MKS_URL" 2>&1 | grep Last-Modified | cut -d' ' -f3- >"$TIMESTAMP"
 ret=$?
 if [ $ret != 0 ]; then
 	rm -f "$FLOCK"
